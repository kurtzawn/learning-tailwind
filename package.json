{
  "name": "tailwind",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i ./src/style.css -o ./dist/style.css",
    "watch:tailwind" : "nodemon  --watch ./src/style.css -x \"yarn run build:css\"",
    "serve":"yarn run watch:tailwind",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.16",
    "tailwindcss": "^3.1.2"
  }
}
